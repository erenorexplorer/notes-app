import pytest
from  unittest.mock import Mock
from app.llm import service

# def test_format_note_returns_llm_output(monkeypatch):

#     fake_choice = Mock()
#     fake_choice.message.content = "# Formatted Note"

#     fake_response = Mock()